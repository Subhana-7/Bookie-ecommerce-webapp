<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    /* General */
body {
  font-family: 'Arial', sans-serif;
  background-color: #f4f4f9;
}

.bg-maroon {
  background-color: #800000;
}


.navbar-brand {
  font-size: 1.5rem;
}

.nav-link {
  color: white;
  padding: 15px;
  font-size: 1.1rem;
}

.nav-link.active {
  background-color: #660000;
}

.nav-link:hover {
  background-color: #660000;
}

.card {
  border: none;
  border-radius: 10px;
}

.card-body {
  padding: 20px;
}

.card-title {
  font-size: 1.2rem;
  color: #333;
}

/* Sidebar styles */
#sidebar {
      height: 100vh;               /* Full height */
      background-color: #800000;    /* Sidebar background */
      padding-top: 15px;
    }

    #sidebar .nav-link {
      color: white;                 /* Default text color */
      padding: 10px 15px;           /* Padding */
      display: flex;                /* Flexbox to align icon and text */
      align-items: center;
      text-decoration: none;        /* Remove underline */
      transition: background-color 0.3s, color 0.3s; /* Smooth transition */
    }

    /* Active link (selected) */
    #sidebar .nav-link.active {
      background-color: #495057;    /* Background for active link */
      color: #f8f9fa;               /* Text color for active link */
      border-radius: 5px;           /* Optional: rounded corners */
    }

    /* Hover effect */
    #sidebar .nav-link:hover {
      background-color: #6c757d;    /* Hover background */
      color: #f8f9fa;               /* Hover text color */
      border-radius: 5px;           /* Optional: rounded corners */
    }

    /* Icon styling */
    #sidebar .nav-link span {
      margin-right: 10px;           /* Add spacing between icon and text */
    }

    .main-content {
      margin-left: 250px;
      padding: 20px;
    }

    @media (max-width: 768px) {
      #sidebar {
        position: relative;
        height: auto;
      }
      .main-content {
        margin-left: 0;
      }
    }

  </style>
</head>
<body>
  <!-- Header -->
  <header class="navbar navbar-dark bg-maroon p-3">
    <a href="#" class="navbar-brand">Zahra</a>
    <div class="d-flex align-items-center">
      <a href="/admin/logout" class="btn btn-outline-light">Logout</a>
    </div>
  </header>