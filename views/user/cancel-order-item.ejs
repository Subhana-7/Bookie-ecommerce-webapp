<!DOCTYPE html>
<html>

<head>
    <title>Cancel Item</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    <style>
        body {
            font-family: 'Segoe UI', system-ui, sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
        }

        .card {
            border-radius: 12px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }

        .item-summary {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .btn-cancel {
            background-color: #dc3545;
            color: white;
            border-radius: 30px;
            font-size: 1rem;
            padding: 10px 20px;
        }

        .btn-cancel:hover {
            background-color: #c82333;
        }

        .btn-cancel:disabled {
            opacity: 0.65;
            cursor: not-allowed;
        }

        .btn-back {
            background-color: #6c757d;
            color: white;
            border-radius: 30px;
            font-size: 1rem;
            padding: 10px 20px;
        }

        .btn-back:hover {
            background-color: #5a6268;
        }
    </style>
</head>

<body>

    <div class="container mt-5">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h5>Cancel Item</h5>
            </div>
            <div class="card-body">
                <h6>Order Details</h6>
                <p><strong>Order ID:</strong> <%= order.orderId %></p>
                <p><strong>Order Date:</strong> <%= order.createdOn.toDateString() %></p>

                <hr>

                <h6>Item to Cancel</h6>
                <div class="item-summary mb-3">
                    <div>
                        <strong><%= item.product ? item.product.productName : item.productName %></strong>
                        <div class="text-muted">Quantity: <%= item.quantity %></div>
                    </div>
                    <div class="font-weight-bold">₹<%= item.price * item.quantity %></div>
                </div>

                <p class="text-muted">
                    Only this item will be cancelled and its amount refunded (if paid online).
                    The rest of your order will remain unaffected.
                </p>

                <hr>

                <h6>Reason for Cancellation</h6>
                <div class="form-group">
                    <textarea id="cancelReason" class="form-control" rows="3"
                        placeholder="Enter your reason for cancellation"></textarea>
                </div>

                <div class="d-flex justify-content-between">
                    <a href="/order-details/<%= order._id %>" class="btn btn-back">Back to Order</a>
                    <button id="confirmCancelItem" class="btn btn-cancel"
                        data-order-id="<%= order._id %>" data-item-id="<%= item._id %>">
                        Confirm Cancellation
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            $('#confirmCancelItem').on('click', function () {
                const $btn = $(this);
                const orderId = $btn.data('order-id');
                const itemId = $btn.data('item-id');
                const reason = $('#cancelReason').val().trim();

                if (!reason) {
                    Swal.fire('Error', 'Please provide a reason for cancellation.', 'error');
                    return;
                }

                Swal.fire({
                    title: 'Are you sure?',
                    text: 'You will not be able to undo this action!',
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonText: 'Yes, cancel it!',
                    cancelButtonText: 'No, keep it'
                }).then((result) => {
                    if (!result.isConfirmed) return;

                    $btn.prop('disabled', true).text('Cancelling...');

                    $.ajax({
                        url: `/cancel-order-item/${orderId}/${itemId}`,
                        method: 'POST',
                        data: { reason },
                        success: function () {
                            Swal.fire('Cancelled!', 'The item has been successfully cancelled.', 'success')
                                .then(() => {
                                    window.location.href = `/order-details/${orderId}`;
                                });
                        },
                        error: function (xhr) {
                            const msg = xhr.responseJSON?.message || 'Something went wrong. Please try again.';
                            Swal.fire('Error!', msg, 'error');
                            $btn.prop('disabled', false).text('Confirm Cancellation');
                        }
                    });
                });
            });
        });
    </script>

</body>

</html>
