<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f4f4;
        }

        .error-card {
            background: rgb(255, 76, 76);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen" style="background-color: black;">
    <div class="w-full max-w-md">
        <div class="error-card text-white rounded-lg shadow-xl p-6">
            <div class="text-center mb-6">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-white mb-4" fill="none"
                    viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <h1 class="text-3xl font-bold mb-2">Payment Failed</h1>
                <p class="text-white opacity-75">We're sorry, your payment could not be completed.</p>
            </div>

            <div class="bg-white bg-opacity-10 rounded-lg p-2 mb-4">
                <h2 class="text-xl font-semibold mb-3">Order Summary</h2>
                <div class="space-y-2">
                    <p class="flex justify-between">
                        <span>Order ID:</span>
                        <span class="font-bold">
                            <%= order.orderId %>
                        </span>
                    </p>
                    <p class="flex justify-between">
                        <span>Payment Status:</span>
                        <span class="font-bold text-red-300">
                            <%= order.paymentStatus %>
                        </span>
                    </p>
                    <p class="flex justify-between">
                        <span>Total Amount:</span>
                        <span class="font-bold">₹<%= order.finalAmount %></span>
                    </p>
                </div>
            </div>

            <div class="bg-white bg-opacity-10 rounded-lg p-4 mb-4">
                <h3 class="text-lg font-semibold mb-3">Products</h3>
                <ul class="space-y-2">
                    <% order.orderedItems.forEach(item=> { %>
                        <li class="flex justify-between">
                            <span>
                                <%= item.product.productName %>
                            </span>
                            <span>Qty: <%= item.quantity %> | ₹<%= item.price %></span>
                        </li>
                        <% }); %>
                </ul>
            </div>

            <div class="text-center">
                <p class="text-white opacity-75 mb-4">Please try again or contact our support team.</p>
                <div class="flex justify-center space-x-4">
                    <a href="/continue-payment/<%= order._id %>"
                        class="bg-white text-red-500 px-4 py-2 rounded-full hover:bg-gray-100 transition">
                        Retry Payment
                    </a>
                    <a href="/orders-list"
                        class="border border-white text-white px-4 py-2 rounded-full hover:bg-white hover:text-red-500 transition">
                        Go to Orders
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>

</html>